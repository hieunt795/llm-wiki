# temp/ — Tool Scratch Space

> File tạm do scripts/pipeline tạo ra. **Không cite, không commit vào wiki.**
> Có thể xóa bất kỳ lúc nào mà không ảnh hưởng đến knowledge base.

## Dùng cho

- Output trung gian của `auto_synthesis.py`, `deepdive_search.py`, v.v.
- Draft chưa được review
- JSON/log files từ pipeline runs

## Không dùng cho

- Research notes có giá trị → dùng `04_outputs/research/`
- Draft wiki nodes → dùng `04_outputs/drafts/`
- Content để publish → dùng `06_publish/`
